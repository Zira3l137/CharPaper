[workspace]
members = ["crates/*"]
resolver = "3"

[workspace.package]
version = "0.1.0"
edition = "2024"

# One place to state a version. Members write `bevy.workspace = true`, so a
# version bump is a one-line change and the crates can never drift apart.
[workspace.dependencies]
bevy = "0.19.1"
raw-window-handle = "0.6"

deskpal-scene = { path = "crates/deskpal-scene" }
deskpal-wallpaper = { path = "crates/deskpal-wallpaper" }
deskpal-windows = { path = "crates/deskpal-windows" }

# Profiles only take effect in the workspace root, so they live here rather
# than in any member manifest.
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = "thin"
codegen-units = 1
